# CLAUDE.md

@AGENTS.md

Follow the contributor rules in `AGENTS.md`. In particular, edit
`plugins/creative-writing-skills/` first, regenerate with
`python3 scripts/sync_claude_distribution.py --apply`, and never hand-edit the
generated `cw/` tree.
