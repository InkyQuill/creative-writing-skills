# Tree and table of contents

Use a table of contents for a long document and a tree when parent-child
relationships matter. Build navigation from headings or an explicit data
model. Use links for destinations and native `<details>` for optional branches.
Keep labels meaningful when read alone, and allow keyboard access without
requiring hover. Do not hide the only route to a page inside a collapsed
control with no visible cue.
