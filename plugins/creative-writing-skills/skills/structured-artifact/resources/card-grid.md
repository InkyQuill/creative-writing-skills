# Card grid

Use cards for a collection whose items each have a short identity and an
optional detail. Show the name and useful summary without interaction. Place
long detail in a linked page or native `<details>` element. Keep the full
card keyboard reachable, avoid nested click targets, and let cards stack in a
single column on narrow screens.
