# Interactive graphs

Use a graph framework only when moving nodes, editing edges, or live graph
filtering is central to the reader's task. Verify the library's current API
and bundle its runtime if offline use is required. Keep a text list of nodes
and connections so the information survives when scripting or interaction
fails. For ordinary dependency maps, use `diagrams.md` instead.
