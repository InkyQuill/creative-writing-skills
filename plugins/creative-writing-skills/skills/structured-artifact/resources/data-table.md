# Data table

Use a native `<table>` for records with consistent columns. Name the columns
with `<th>` and add a caption when the subject is not obvious. For a large
set, offer search or sorting only if readers need it; preserve the original
data order as a usable fallback. On small screens, allow horizontal scrolling
without clipping cells or replacing column labels with unexplained icons.
