# Multi-page static site

Use child pages when each topic needs its own substantial explanation. Make
`index.html` a useful map, give every child page a clear opening and a path
back, and share one stylesheet when that reduces duplication. Keep links
relative so the folder works from `file://` as well as a web server. Open
each page directly during validation; navigation from the index alone does
not reveal missing assets on a child page.
