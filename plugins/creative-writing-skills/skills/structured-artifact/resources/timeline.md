# Timeline view

Use a timeline when event order or gaps matter. Put dates or sequence labels
in text and keep each event readable without hover. Group dense periods
instead of shrinking labels to fit. A native list is often enough; add a
visual line or horizontal interaction only if it improves orientation.
