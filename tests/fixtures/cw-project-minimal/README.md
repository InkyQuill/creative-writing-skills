# Minimal story-project fixture

Runtime tests materialize this fixture with `cwcli.scaffold.render_scaffold()` so the generated scaffold has one canonical definition and is not duplicated in test data.
